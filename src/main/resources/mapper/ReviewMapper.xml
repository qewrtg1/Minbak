<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minbak.web.review.ReviewMapper">


    <!-- 리뷰 목록 조회 -->
    <select id="findAllReview" resultType="com.minbak.web.review.ReviewDto">
        SELECT * FROM review
    </select>

    <!-- 리뷰 목록 순서대로 조회 -->
    <select id="findOrderedReview" resultType="com.minbak.web.review.ReviewDto">
        SELECT * FROM review
        ORDER BY `order` ASC
    </select>

    <!-- 리뷰 상세 조회 (특정 리뷰 조회) -->
    <select id="findReviewById" resultType="com.minbak.web.review.ReviewDto">
        SELECT * FROM review
        WHERE review_id = #{reviewId}
    </select>

    <!-- 리뷰 수정 -->
    <update id="updateReview">
        UPDATE review
        SET
        content = #{content},
        score = #{score},
        updated_at = NOW()
        WHERE review_id = #{reviewId}
    </update>

    <!-- 리뷰 삭제 -->
    <delete id="deleteReview">
        DELETE FROM review
        WHERE review_id = #{reviewId}
    </delete>

</mapper>